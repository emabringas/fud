include_directories( ../src/common )
include_directories( ../src/client )
include_directories( ../src/server )

add_subdirectory( counter )
add_subdirectory( counter-msg )
add_subdirectory( file-size-counter )
